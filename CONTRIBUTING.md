# 기여

**개인용(n=1) 저널 앱이다.** 한 사람이 쓰고 한 사람이 읽는다. 그래도 코드를 공개해 둔
이상, **작은 개선은 반갑다.** 다만 범위가 좁으니 손대기 전에 이 문서를 읽어 헛수고를
피하기 바란다.

이 앱이 하는 일은 **두 개뿐**이다 — **복붙을 없앤다**, **에너지 레벨을 추적한다.** 어떤
변경이든 둘 중 하나에 닿아야 한다. 판정 기준의 정본은 [AGENTS.md](./AGENTS.md)다.

## 시작하기

```bash
npm install
npm run dev     # Cloudflare 계정 불필요
npm run gate    # 보내기 전에
```

계정 없이 어디까지 되는지는 [README의 「로컬에서 돌리기」](./README.md)에 표로 있다.
요약하면 **동기화(`/api/*`)만 안 되고 나머지는 다 된다.**

## 반가운 기여

**순수 함수 계층이 가장 손대기 좋다.** 넷 다 저장소도 네트워크도 DOM도 모르고 테스트가
붙어 있어서, 브라우저 없이 `npm test`만으로 판정된다.

| 파일 | 하는 일 |
|---|---|
| [markdown.js](./src/lib/markdown.js) | 저널 형식의 파싱·조립. **`assemble(parse(x)) === x`가 계약이다** (파일 끝 개행 개수는 예외) |
| [merge.js](./src/lib/merge.js) | 동기화 병합 규칙. 블록 단위 LWW + 진 쪽을 `conflict` 사본으로 |
| [series.js](./src/lib/series.js) | 그래프 좌표 계산. 결측일에 선을 끊는다 |
| [date.js](./src/lib/date.js) | KST 캘린더 날짜. 날짜와 순간을 섞지 않는다 |
| [state.svelte.js](./src/lib/state.svelte.js) | 화면 상태와 동기화. 룬을 쓰지만 [하네스](./src/lib/state.harness.js)로 Node에서 돈다 |

그 밖에:

- **버그 리포트.** 재현 절차와 실제/기대 동작이면 충분하다. **저널 본문은 붙이지 말 것** —
  형식만 보여주면 된다.
- **모바일 UI 문제.** 오터치·레이아웃 밀림·키보드에 가려지는 입력처럼 폰에서만 보이는
  것들. 이 앱은 폰에서 쓰라고 만든 것이라 여기가 가장 아프다.
- **접근성·성능·의존성 제거.** 런타임 의존성은 **0개**이고 그 상태를 유지한다.
- **오타·문서 오류.** 문서가 코드보다 오래 사는 리포다.

## 받지 않는 것

거절이 아니라 **미리 아끼자는 뜻**이다. 아래는 방침이지 판단 대기 중인 항목이 아니다.

1. **제품 범위 확장** — 멀티유저·공유·회원가입·유료화·바이럴. 전부 [AGENTS.md](./AGENTS.md)의
   「이 리포의 성격」에서 범위 밖으로 못박혀 있다.
2. **설계 불변식을 어기는 변경** — 넷은 [AGENTS.md](./AGENTS.md)의 「설계 불변식」에 있다.
   요약: **오프라인이 기본** · **동기화는 사람이 누른다**(상시 자동 push 없음) ·
   **사용자가 쓴 글자를 잃지 않는다** · **앱에 LLM을 넣지 않는다.**
3. **앱 내 LLM 기능** — 요약·자동 작성·감정 분석 전부. 저널 쓰기는 그 자체가 목적인
   행위라, AI가 대신 쓰면 과정의 가치가 지워진다. **영구적으로 없다.**
4. **큰 리팩터·프레임워크 교체·TypeScript 이관.** 스택은 고정이다 — Svelte 5 + Vite,
   `.js` + JSDoc + `checkJs`, **Prettier 안 씀.** 포맷만 바꾸는 PR은 신호를 잡음에 묻는다.
5. **차트/유틸 라이브러리 추가.** 의존성 하나를 넣기 전에 직접 짜면 몇 줄인지 세어 본다 —
   그래프도 SVG로 직접 그렸다.
6. **CI·훅 자동화.** 게이트는 사람이 부르는 명령형 버튼이다.

기능을 늘리는 제안에는 두 질문이 먼저 붙는다: **「기존 수단의 조합으로 안 되는가?」**와
**「이건 어떻게 다시 읽히나?」** 인출 통로 없는 저장 기능은 설계 미완성이다.

## 보내기 전에

- **`npm run gate`를 통과시킬 것** (`test` → `reach` → `lint` → `check` → `build`).
  계정 없이 돈다.
- **새 프로덕션 파일에는 테스트가 붙어야 한다.** `reach`가 「테스트가 로드하지 않는
  파일」 목록이 늘어나면 실패시킨다. 순수 함수는 그냥 붙이면 되고, `state.svelte.js`처럼
  룬을 쓰는 파일은 [하네스](./src/lib/state.harness.js)가 이미 있다.
- **테스트를 붙일 것.** 순수 함수를 고쳤으면 같은 이름의 `*.test.js`에 케이스를 더한다.
  **픽스처는 지어낸 문장을 쓴다** — 실제 저널을 복사하지 않는다.
- **커밋을 좁게.** 한 커밋이 한 가지를 한다.
- **마크다운 링크는 `./` 또는 `../`로 시작하고 실재해야 한다** ([lint](./scripts/check-docs.mjs)가
  검사한다).

## 언어

- **사람이 읽는 것은 한국어** — 문서·커밋 메시지·이슈·PR 설명.
- **코드·식별자·주석은 영어.** 도메인 용어가 영어로 자연스러우면 영어를 쓴다
  (`energy`, `cognitive`, `emotional`, `physical`).

한국어가 불편하면 영어로 열어도 좋다. 읽는다.

## 이 리포의 사적인 데이터

[references/sample.md](./references/sample.md)는 **지어낸 형식 예시다** — 자유롭게 인용하고
고쳐도 된다. 다만 **실제 저널 기록을 리포에 넣지 않는다.** 이슈·로그·커밋에 저널 본문이
새지 않게 한다.
